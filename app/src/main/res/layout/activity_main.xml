<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
            name="viewModel"
            type="com.lab02.proyectobase.viewmodel.MainViewModel" />
    </data>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".ui.MainActivity">

        <TextView
            android:id="@+id/title_system"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="50dp"
            android:fontFamily="sans-serif-black"
            android:textColor="@color/green_normal"
            android:textSize="30sp"
            android:text="Sistema CanCare"
            android:textAlignment="center"
            android:gravity="center" />

        <EditText
            android:id="@+id/edt_email"
            android:layout_width="300dp"
            android:layout_height="40dp"
            android:layout_centerHorizontal="true"
            android:layout_below="@+id/title_system"
            android:layout_marginTop="90dp"
            android:hint="Email"
            android:paddingLeft="10dp"
            android:textSize="15dp"
            android:text="@={viewModel.correo}"/>

        <EditText
            android:id="@+id/edt_password"
            android:layout_width="300dp"
            android:layout_height="40dp"
            android:layout_centerHorizontal="true"
            android:layout_below="@+id/edt_email"
            android:layout_marginTop="30dp"
            android:hint="Password"
            android:paddingLeft="10dp"
            android:textSize="15sp"
            android:text="@={viewModel.password}"/>

        <Button
            android:id="@+id/btn_login"
            android:layout_width="200dp"
            android:layout_height="wrap_content"
            android:layout_centerHorizontal="true"
            android:layout_marginTop="40dp"
            android:layout_below="@+id/edt_password"
            android:text="Iniciar Sesion"
            android:onClick="@{()->viewModel.iniciarSesion()}"
            android:textSize="15sp"
            android:textAllCaps="false"
            android:textColor="#ffffff"
            android:background="#236fdd" />
        <Button
            android:id="@+id/btn_registrarse"
            android:layout_width="200dp"
            android:layout_height="wrap_content"
            android:layout_centerHorizontal="true"
            android:layout_marginTop="40dp"
            android:layout_below="@+id/btn_login"
            android:text="Registrarse"
            android:onClick="@{()->viewModel.addCliente()}"
            android:textSize="15sp"
            android:textAllCaps="false"
            android:textColor="#ffffff"
            android:background="#236fdd" />

        <Button
            android:id="@+id/btn_invited"
            android:layout_width="200dp"
            android:layout_height="wrap_content"
            android:layout_centerHorizontal="true"
            android:layout_marginTop="20dp"
            android:layout_below="@+id/btn_registrarse"
            android:text="Invitado"
            android:textSize="15sp"
            android:textAllCaps="false"
            android:textColor="#ffffff"
            android:onClick="@{()->viewModel.goToMenu()}"
            android:background="#236fdd" />
        <TextView
            android:id="@+id/aux"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="50dp"
            android:layout_below="@+id/btn_invited"
            android:fontFamily="sans-serif-black"
            android:textColor="@color/green_normal"
            android:textSize="10sp"
            android:textAlignment="center"
            android:gravity="center"
            android:text="@={viewModel.text}"/>
        <TextView
            android:id="@+id/auxBase"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="50dp"
            android:layout_below="@+id/aux"
            android:fontFamily="sans-serif-black"
            android:textColor="@color/green_normal"
            android:textSize="10sp"
            android:textAlignment="center"
            android:gravity="center"
            android:text="@={viewModel.textBase}"/>

    </RelativeLayout>
</layout>